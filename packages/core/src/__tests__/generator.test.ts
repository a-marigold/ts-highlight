import { describe, it, expect } from 'bun:test';

import { tokenize } from '../tokenizer';
import { generate } from '../generator';

describe('generator', () => {
    it('should return a correct generated html', () => {});
});
